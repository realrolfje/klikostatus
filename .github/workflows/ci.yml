name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    name: Validate integration
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Compile Python files
        run: python -m compileall custom_components/kliko_status scripts/test_kliko.py

      - name: Validate JSON files
        run: |
          python -m json.tool hacs.json > /dev/null
          python -m json.tool custom_components/kliko_status/manifest.json > /dev/null
          python -m json.tool custom_components/kliko_status/translations/en.json > /dev/null
          python -m json.tool custom_components/kliko_status/translations/nl.json > /dev/null

      - name: Validate repository metadata
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          manifest = json.loads(Path("custom_components/kliko_status/manifest.json").read_text())
          hacs = json.loads(Path("hacs.json").read_text())

          assert manifest["domain"] == "kliko_status"
          assert manifest["name"] == hacs["name"]
          assert manifest["version"]
          PY
